# ─────────────────────────────────────────────────────────────
# ai-server: Runtime dependencies (Mac + conda, Python 3.11)
# 이 파일은 "서비스가 실제로 사용하는 패키지"만 고정합니다.
# - FastAPI 웹 서버 구동
# - 설정 및 환경변수 관리
# 주의: 하위 의존성은 pip가 자동으로 설치합니다.
# ─────────────────────────────────────────────────────────────

# --- Web framework / ASGI server ---
fastapi==0.116.1           # 고성능 ASGI 웹 프레임워크
uvicorn[standard]==0.35.0  # ASGI 서버(uvloop 등 포함 번들)

# --- Settings / .env / Validation ---
pydantic==2.11.7           # DTO/스키마 검증
pydantic-settings==2.10.1  # .env 기반 설정 로딩
python-dotenv==1.1.1       # .env 파싱(보조적으로 사용 시)

# --- NLP / 모델 추론 ---
torch==2.8.0               # PyTorch (모델 추론/학습; Apple Silicon은 MPS 가속 가능)
safetensors==0.6.2         # 안전한 텐서 포맷
nltk==3.9.1                # 텍스트 전처리/토큰화 등(필요 시)

# --- clustering 사용 ===
scikit-learn==1.7.2
# --- (선택) 요청 편의: requests를 직접 사용할 경우에만 활성화 ---
# requests==2.32.5
pytest
flake8
